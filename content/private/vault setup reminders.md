Obsidian configurations are ignored with git. If this is a fresh
clone, make sure to go to `Settings` -> `Files & LInks`:
- Set `New link format` to `Absolute path in vault`
- Toggle on `Automatically update internal links`
